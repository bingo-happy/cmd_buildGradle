buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:1.5.0'
    }
	  
	  
}
task wrapper(type: Wrapper) {
    gradleVersion = '2.1'  //gradle 的版本
}

 
task clean(type: Delete) {
    delete rootProject.buildDir
}
